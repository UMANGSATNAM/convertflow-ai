<style>
.cf-fc-04-v7 {
  padding: {{ section.settings.padding_y }}px 20px;
  background-color: {{ section.settings.bg_color }};
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
  overflow: hidden;
}

.cf-fc-04-v7-inner {
  max-width: {{ section.settings.max_width }}px;
  margin: 0 auto;
}

.cf-fc-04-v7-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-end;
  margin-bottom: 50px;
}

.cf-fc-04-v7-title {
  color: {{ section.settings.text_color }};
  font-size: 46px;
  font-weight: 900;
  font-style: italic;
  text-transform: uppercase;
  margin: 0;
  letter-spacing: -0.02em;
}

.cf-fc-04-v7-title span {
  color: {{ section.settings.accent_color }};
}

.cf-fc-04-v7-link {
  color: {{ section.settings.accent_color }};
  text-decoration: none;
  font-weight: 800;
  font-size: 14px;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  display: flex;
  align-items: center;
  gap: 5px;
  transition: transform 0.2s;
}

.cf-fc-04-v7-link:hover {
  transform: translateX(5px);
}

.cf-fc-04-v7-grid {
  display: grid;
  grid-template-columns: repeat({{ section.settings.cols_desktop }}, 1fr);
  gap: 30px;
}

.cf-fc-04-v7-card {
  text-decoration: none;
  display: block;
}

.cf-fc-04-v7-image-wrap {
  width: 100%;
  aspect-ratio: 4/5;
  background: {{ section.settings.card_bg }};
  position: relative;
  overflow: hidden;
  margin-bottom: 20px;
  clip-path: polygon(5% 0, 100% 0, 95% 100%, 0% 100%);
  transition: transform 0.3s, background-color 0.3s;
}

.cf-fc-04-v7-card:hover .cf-fc-04-v7-image-wrap {
  transform: translateY(-10px);
  background: #1f2937;
}

.cf-fc-04-v7-image-wrap img {
  position: absolute; top: 0; left: 0; width: 100%; height: 100%; object-fit: cover;
  transition: transform 0.5s;
}

.cf-fc-04-v7-card:hover .cf-fc-04-v7-image-wrap img {
  transform: scale(1.1);
}

.cf-fc-04-v7-badge {
  position: absolute;
  top: 15px; left: 20px;
  background: {{ section.settings.accent_color }};
  color: #fff;
  font-size: 11px;
  font-weight: 900;
  padding: 4px 10px;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  transform: skewX(-10deg);
}

.cf-fc-04-v7-name {
  color: {{ section.settings.text_color }};
  font-size: 20px;
  font-weight: 900;
  font-style: italic;
  text-transform: uppercase;
  margin: 0 0 5px 0;
}

.cf-fc-04-v7-price {
  color: rgba(255,255,255,0.7);
  font-size: 16px;
  font-weight: 600;
}

@media (max-width: 900px) {
  .cf-fc-04-v7-grid { grid-template-columns: repeat(2, 1fr); }
  .cf-fc-04-v7-header { flex-direction: column; align-items: flex-start; gap: 20px; }
}
@media (max-width: 600px) {
  .cf-fc-04-v7-grid { grid-template-columns: repeat(2, 1fr); gap: 15px; }
  .cf-fc-04-v7-title { font-size: 32px; }
  .cf-fc-04-v7-name { font-size: 16px; }
}
</style>

<div class="cf-fc-04-v7">
  <div class="cf-fc-04-v7-inner">
    <div class="cf-fc-04-v7-header">
      <h2 class="cf-fc-04-v7-title">{{ section.settings.title | replace: '[', '<span>' | replace: ']', '</span>' }}</h2>
      <a href="{{ section.settings.btn_link }}" class="cf-fc-04-v7-link">{{ section.settings.btn_text }} &rarr;</a>
    </div>

    <div class="cf-fc-04-v7-grid">
      {% for block in section.blocks %}
        <a href="{{ block.settings.url }}" class="cf-fc-04-v7-card">
          <div class="cf-fc-04-v7-image-wrap">
            {% if block.settings.badge != blank %}
              <div class="cf-fc-04-v7-badge">{{ block.settings.badge }}</div>
            {% endif %}
            {% if block.settings.image != blank %}
              <img src="{{ block.settings.image | image_url: width: 600 }}" alt="{{ block.settings.title }}">
            {% else %}
               <div style="width:100%;height:100%;display:flex;align-items:center;justify-content:center;color:#475569;">Img</div>
            {% endif %}
          </div>
          <h3 class="cf-fc-04-v7-name">{{ block.settings.title }}</h3>
          <div class="cf-fc-04-v7-price">{{ block.settings.price }}</div>
        </a>
      {% endfor %}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "07 · Active Products",
  "settings": [
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "card_bg",
      "label": "Card Image BG",
      "default": "#111827"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text Color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "accent_color",
      "label": "Accent Color",
      "default": "#f97316"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "TOP [PERFORMERS]"
    },
    {
      "type": "text",
      "id": "btn_text",
      "label": "Button Name",
      "default": "Shop All Gear"
    },
    {
      "type": "url",
      "id": "btn_link",
      "label": "Link"
    },
    {
      "type": "range",
      "id": "cols_desktop",
      "label": "Columns (Desktop)",
      "min": 2,
      "max": 5,
      "step": 1,
      "default": 4
    },
    {
      "type": "range",
      "id": "padding_y",
      "label": "Vertical Padding",
      "min": 40,
      "max": 160,
      "step": 5,
      "default": 80
    },
    {
      "type": "range",
      "id": "max_width",
      "label": "Max Width",
      "min": 800,
      "max": 1600,
      "step": 50,
      "default": 1200
    }
  ],
  "blocks": [
    {
      "type": "product",
      "name": "Product",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Product Title",
          "default": "Hydration Matrix"
        },
        {
          "type": "text",
          "id": "price",
          "label": "Price",
          "default": "$39.99"
        },
        {
          "type": "text",
          "id": "badge",
          "label": "Badge",
          "default": "New"
        },
        {
          "type": "url",
          "id": "url",
          "label": "Product Link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "07 · Active Pulse Product",
      "blocks": [
        {
          "type": "product"
        },
        {
          "type": "product"
        },
        {
          "type": "product"
        },
        {
          "type": "product"
        }
      ]
    }
  ]
}
{% endschema %}
